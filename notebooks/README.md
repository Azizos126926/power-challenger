# Notebook archive

The original repository relied heavily on notebooks for experimentation.  
To keep the project transparent while making the main codebase easier to review, all original notebooks have been preserved under `notebooks/archive/`.

Use the Python package in `src/` for the cleanest view of the final pipeline.
